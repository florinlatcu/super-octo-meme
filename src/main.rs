fn main() {
    let mut my_name: String = "Dave".to_string();
    my_name.push('!');
    println!("{}", my_name);
    // Rust programs starts with fn main()
    // You put the code inside a block. It starts with { and ends with }
}
